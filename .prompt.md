# This file is used to scrap the data from the investorgain site.


** For the ipo investorgain we create a separate file and separate function **

we have a ipo scrap file the file name is `scrap_investorgain_ipo_data.py` and this file is used to scrap the data from the investorgain site.
  1. i want to scrap dat from the site.
  2. i want to formate the data into a more readable format. we can scall the function name as `format_investorgain_ipo_data` for the formate ipo row data. 
3. bellow the work we have to do intot the file code structure wise.
   -> we have to scrap the data from the site. so we can use the `NSEInvestorGainIPOController` class from the existing code.
   -> we have to formate the data into a more readable format. so we can use the `format_investorgain_ipo_data` function from the existing code.
   -> we can CONFIGURATION the file to scrap the data from the site. so we can use the `COMMON_HEADERS` and `COMMON_PARAMS` from the existing code.
   -> we can use the `get_logger` function from the existing code to log the data.
   -> the UTILS file we can create once utiles file ad add the functions thjose all into the once utiles saprate file for the ipo data utilits and use this code stuctured wise.
    -> in to the scrap_investorgain_ipo_data.py file we can use the we can use the same stuchure ilke others controler file. like frist we scrap all the ipo's data then we formate the data (call the `format_investorgain_ipo_data` function) and then we save the data into a file and database.
    -> here save data fnction is diff then others so we cretae a saprate function for the ipo data . thet functon i describe bellow.

4. we can use the `save_investorgain_ipo_data` function to save the data into a file and database.
   then we can save the data as well as we can save the data into a database and file. but the save time we cant delete the old data we can update the old dat with the scraped new data . so how we can do this.
    -> we create a new function saprate for the ipo scrap data save function.
    -> when we update the exsiting ipo data so how we can find the exsiting data in the database and file. so for that we have a ipo id when we scrap so that time we can scrap also the ipo id so we can map the data with the ipo id and update the data in the database and file.

5. if you need so create a only once controler file for the ipo data scrap, if not so edit code inot in the existing file `scrap_investorgain_ipo_data.py` file.

6. we have to create a new file into the test dir for the ipo data scrap. the file name is `scrap_investorgain_ipo_data.py`.

7. **Update the `cron_jobs.py` file**: In the `Services/cron_jobs.py` file, import the `NSEInvestorGainIPOController` class and call the `scrap_investorgain_ipo_data` function to schedule the scraping job. This will ensure that the IPO data is scraped at regular intervals as defined in the cron job.

# we save the data scraped after the formate data from the file and database. collection name is `investorgain_ipo_data_v1` and the file name is `investorgain_ipo_data_v1.json`. 


** NOTE  : WHEMWE EDIT THE CODE FOR THE THIS FILE SO CARE FULLY WE DONT NEED TO CHANGE SCRAP LOGIC WHNEVER WE DO NOT THE SCRAP ACTUALLY DATA FROM THE SITE SO WE DOMNT CHNAGE THE SCRAP LOGIC FUNCTIONALITY  **